name: qargo
type: postgres
options:
  host: ${SUPABASE_HOST}
  port: ${SUPABASE_PORT}
  database: ${SUPABASE_DBNAME}
  user: ${SUPABASE_USER}
  password: ${SUPABASE_PASSWORD}
  ssl:
    rejectUnauthorized: false
